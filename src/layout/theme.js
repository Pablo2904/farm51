import { colors, screenHeight } from './variables'
import { flexCenter, flexSpaceBeetwen, flexColumn } from './mixins'

export const theme = {
  colors,
  screenHeight,
  flexCenter,
  flexSpaceBeetwen,
  flexColumn
}
